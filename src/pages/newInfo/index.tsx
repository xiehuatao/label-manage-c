import React from 'react';
import {withRouter} from "react-router-dom";


function NewInfo() {

    return (
        <div>
            newInfo
        </div>
    )
}

export default withRouter(NewInfo);